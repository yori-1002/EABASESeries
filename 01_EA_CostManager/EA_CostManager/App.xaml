<Application x:Class="EA_CostManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:EA_CostManager.Converters">
    <!--
    ▼ 修正（v1.0.2）：StartupUri="Views/MainWindow.xaml" を削除
    
    【理由】
    起動時にスプラッシュウィンドウを表示してから MainWindow を出す制御に変更したため、
    App.xaml.cs の OnStartup の最後で new MainWindow().Show() を明示的に呼ぶ運用にする。
    StartupUri が残っていると MainWindow が自動起動してスプラッシュより先に出てしまう。
    -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Assets/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <conv:BoolToVisibilityConverter x:Key="bool_to_vis" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
